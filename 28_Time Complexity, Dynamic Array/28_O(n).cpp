#include<bits/stdc++.h>
using namespace std;
int main()
{
    int count;
    cin >> cout;
    int arr[count];
    for( int i = 0; i < count; i++ )
    {
        cin >> a[i];
    }
    int sum = 0;
    for ( int i = 0; i < count; i *= 2 )
    {
        s += a[i];
    }

    cout << sum << endl;
}    

    // 10^5 ------ 0.01s
    // 10^6 ------ 0.1s
    // 10^7 ------ 1s (defined)
    // 10^8 ------ 10s 
    // 10^9 ------ 100s